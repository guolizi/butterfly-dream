# 🧪 Butterfly Dream 综合记忆评测

> 公平、全面、不迁就被测系统的记忆系统评测框架。

## 设计原则

1. **公平公正** — 查询使用自然语言问句，不迁就被测系统的检索策略
2. **全面覆盖** — 测试检索、推理、时序、跨语言、压力等多维度能力
3. **难度梯度** — 从基础检索到多跳推理，难度递进
4. **可复现** — 确定性 seed + 临时数据库，每次运行结果一致

## 文件结构

```
eval/
├── README.md            ← 本文档
├── run_eval.py          ← 评测运行器
├── scenarios.json       ← 主场景集 (23 场景, 59 查询)
├── baseline.json        ← 基线结果
└── gen_stress_test.py   ← 大规模压力测试生成器
```

## 评测维度一览

| 维度 | 场景数 | 查询数 | 来源基准 | 测试内容 |
|:-----|:------:|:------:|:---------|:---------|
| 🔍 基础检索 | 3 | 5 | 通用 | EN/ZH/混合语言关键词匹配 |
| 🧬 实体探针 | 1 | 1 | MEMOBENCH | 按实体名检索全部事实 |
| 🕰️ 时间线查询 | 1 | 1 | LoCoMo | 实体事实的时间排序 |
| 🗑️ 跨源去重 | 1 | 1 | — | Jaccard ≥ 0.7 跳重 |
| 🔒 持久标记过滤 | 1 | 1 | — | `persistent_only` 过滤 |
| ⭐ 重要性排序 | 1 | 1 | — | 高重要事实优先排序 |
| 🎯 矛盾检测 | 1 | 1 | — | 两两矛盾检测 |
| 🌀 遗忘曲线 | 2 | 6 | LongMemEval | 0/5/15 干扰后检索 |
| 🔗 联想记忆 | 1 | 5 | MEMOBENCH | 线索词→目标事实 |
| ✏️ 记忆编辑 | 1 | 4 | MemoryBench | 信息更新后查最新 |
| 🔄 同义改写鲁棒性 | 2 | 8 | MemoryBench | 同一事实不同关键词变体 |
| 🔗 跨会话检索 | 1 | 3 | LoCoMo | 分阶段添加信息再检索 |
| 📊 大规模压力 | 1 | 5 | LongMemEval | 50 事实库中检索 |
| 🧩 多跳推理 | 1 | 3 | LoCoMo | 组合相关事实查询 |
| 🕰️ 时序比较 | 1 | 3 | MEMOBENCH | 按时间范围检索事件 |
| 🎯 对抗相似 | 1 | 2 | MemoryBench | 高度相似事实的精确检索 |
| 🌐 跨语言检索 | 1 | 4 | — | EN→ZH / ZH→EN 跨语言 |
| 🎯 更新精度 | 1 | 4 | MemGPT | 更新一条不影响其他 |

## 快速运行

```bash
# 标准评测 (所有场景)
python3 eval/run_eval.py

# 仅跑特定维度
python3 eval/run_eval.py --name "遗忘曲线"
python3 eval/run_eval.py --name "中文"
python3 eval/run_eval.py --name "英文"

# JSON 输出 (用于对比历史基线)
python3 eval/run_eval.py --json

# 大规模压力测试 (额外场景)
python3 eval/gen_stress_test.py --count 500
python3 eval/run_eval.py --name "500" --extra-scenarios eval/stress_test.json
```

## 基线结果 (v1.0)

| 指标 | 值 | 说明 |
|:-----|:--:|:-----|
| 场景数 | 23 | |
| 查询数 | 59 | |
| **R@1** | **0.735** | 首条命中率 |
| R@3 | 0.924 | 前三命中率 |
| 精确率 | 0.449 | OR 宽召回带来的噪声 |
| 平均延迟 | 7.66ms | 含完整三维评分 |

## 指标说明

| 指标 | 计算方式 |
|:-----|:---------|
| **R@k** | 前 k 条结果中，包含所有预期子串的比例 |
| **精确率** | 前 10 条结果中，相关结果(匹配预期且非预期排除)的比例 |
| **F1** | R@5 与精确率的调和平均 |
| **延迟** | 每次查询的 wall-clock 时间 (ms) |

## 评测结果解读

- **R@1 高** → 系统能准确找到最相关的事实
- **R@3 高但 R@1 低** → 相关事实在候选池中但排名不够靠前（HRR + Jaccard 排序可优化）
- **精确率低** → OR 语义带来过多无关候选，排名无法完全过滤
- **遗忘曲线平坦** → 适度干扰下检索可靠；大规模扰动才可能出现衰减

## 扩展指南

添加新场景：
1. 在 `scenarios.json` 中添加 JSON 对象
2. 每个场景需包含 `name`、`setup`、`queries` 三个字段
3. 查询可使用 `extra_setup` 在查询间添加额外事实（用于遗忘曲线、编辑等场景）
4. 运行 `python3 eval/run_eval.py --json > eval/baseline.json` 更新基线
