# Butterfly Dream 评测管线 — 统一模型配置
# 所有 eval 脚本从这里读取模型设置，换模型只改这一个文件

extraction:
  provider: openrouter
  model: nvidia/nemotron-3-ultra-550b-a55b:free

answer:
  provider: openrouter
  model: nvidia/nemotron-3-ultra-550b-a55b:free

judge:
  provider: openrouter
  model: nvidia/nemotron-3-ultra-550b-a55b:free

# DeepSeek V4 Flash 备选（reasoning model，judge 需要 max_tokens >= 500）
# extraction:
#   provider: deepseek
#   model: deepseek-v4-flash
# answer:
#   provider: deepseek
#   model: deepseek-v4-flash
# judge:
#   provider: deepseek
#   model: deepseek-v4-flash
