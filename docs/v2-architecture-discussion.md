# 🦋 Butterfly Dream v2 — 顶层架构设计讨论

> 开始时间：2026-06-12
> 参与者：guolizi（主人）、Neko-chan
> 状态：讨论中
> 范围：记忆系统的概念架构，非代码实现结构

---

## 一、v1 当前的概念模型

```
输入（对话/消息）
    ↓
LLM 提取 → 原子事实（flat pool）
    ↓
三维评分（relevance × recency × importance）
    ↓
检索 → top-N 事实
```

**v1 的隐含假设：**
- 记忆 = 一堆原子事实的集合
- 所有事实存在同一个平面空间里
- 提取是单向的：对话 → 事实
- 检索是 query-driven 的：问什么搜什么
- 事实之间只有实体 co_occur 关系

---

## 二、v2 完整层次架构

```
L5: 灵魂层（人物还原）
    ────────────────────────────────────
    目标：还原出"这个人"
    内容：综合的人物理解、核心特质、价值观、人生主线
    构建：从 L2-L4 综合提炼，按需动态构建
    例子："Caroline 是一个从自我接纳走向帮助他人的坚韧女性"
    ↑

L4: 叙事层（故事线）
    ────────────────────────────────────
    内容：有起承转合的故事线
    构建：从 L2 的时间链+因果链+情感轨迹动态构建
    特点：不同 query 角度可构建不同叙事
    例子："Caroline 2023年的成长故事：寻找自我→融入社群→遭遇挫折→新的方向"
    ↑ 抽象管道喂养

L3: 抽象层（管道，非静态存储）
    ────────────────────────────────────
    模式发现：事件池 → 行为模式池 / 情感模式
    知识归纳：事件池 → 静态知识池（abstracts_from）
    信念形成：综合 → 灵魂层素材
    ↑ 关系构建

L2: 关系层
    ────────────────────────────────────
    时间链 · 因果链 · 实体图 · 溯源 · 情感轨迹
    ↑ 事实关联

L1: 池（事实分类管理）
    ────────────────────────────────────
    事件记录池 | 静态知识池 | 行为模式池
    + 情感维度（独立维度，非池）
    ↑ 提取+分类

L0: 工作记忆
    ────────────────────────────────────
    原始对话轮次 + 微事实索引（FTS5 关键词检索）

遗忘机制 ←━━━━━━━━━━━━━━━━━━━━━━━━━━━━→ 贯穿所有层
```

---

## 三、各层详解

### L0：工作记忆

| 议题 | 决策 |
|------|------|
| **存储内容** | 原始对话轮次 + 微事实索引（关键词→turn_id→snippet，指向原文位置） |
| **微事实提取** | jieba 分词 + 停用词过滤 + 名词性短语规则（纯本地，零 API） |
| **检索方式** | FTS5 关键词匹配（BM25 × 0.7 + 时间衰减 × 0.3） |
| **检索路由** | 默认：先搜工作记忆 → 匹配不足时搜长期记忆。Agent 可显式指定 `memory="working"` / `"longterm"` / `"all"` |
| **清理策略** | 待定（与架构不相关，实现时再定） |

**微事实索引示意：**

```
原始对话轮次表：
┌──────────┬───────┬──────────────────────────────┐
│ turn_id  │ role  │ content                      │
├──────────┼───────┼──────────────────────────────┤
│ 42       │ user  │ "Caroline 喜欢什么活动？"      │
│ 43       │ asst  │ "她喜欢画画和弹钢琴..."        │
└──────────┴───────┴──────────────────────────────┘

微事实索引表：
┌──────────┬──────────┬──────────────────────────┐
│ keyword  │ turn_id  │ snippet                  │
├──────────┼──────────┼──────────────────────────┤
│ Caroline │ 42,43    │ "Caroline 喜欢什么活动"   │
│ 画画     │ 43       │ "她喜欢画画和弹钢琴"      │
│ 钢琴     │ 43       │ "她喜欢画画和弹钢琴"      │
└──────────┴──────────┴──────────────────────────┘
```

---

### L1：池（事实分类管理）

**划分依据：二维矩阵**

|  | 时间锚定（具体） | 去时间化（稳定） |
|--|---------------|----------------|
| **具体事实** | **事件记录池** | **静态知识池** |
| **行为模式** | （少见，暂不设） | **行为模式池** |

**三个池定义：**

| 池 | 含义 | 评分侧重 | 生命周期 | 上层能力 |
|----|------|---------|---------|---------|
| **事件记录池** | 有时间锚点的具体事件 | recency | 可遗忘/合并 | 事件回溯 |
| **静态知识池** | 去时间化的稳定知识 | importance | 永久保留 | 人物画像、知识问答 |
| **行为模式池** | 从多次事件归纳的条件-行为模式 | relevance+importance | 保留，可更新 | 行为预测 |

**存储方式：** 概念池独立，事实统一存储（`facts` 表，`type` 字段区分池）

**池间关系：多对多映射**
```
多个事件事实 → 联合抽象 → 一个知识/模式事实
一个事件事实 → 不同上下文 → 多个不同角度的抽象
```
映射通过 `fact_relations` 的 `abstracts_from` 关系记录，含 `context` 字段标注抽象角度。

---

### 情感维度（独立维度，非池）

情感作为独立维度，不存储为具体事实，而是存储**结构化的情感轨迹**。

**三个层次：**

```
情感状态节点（时间点 → 情感状态 + 强度）
    ↓ 关联
情感转变（从什么状态到什么状态 + 触发因素）
    ↓ 抽象
情感模式（多次转变中归纳的规律）
```

**数据结构：**

```
emotion_states:
  state_id, person, timestamp, emotion_label, intensity,
  context_fact_id (关联到触发该情感的事实), notes

emotion_transitions:
  transition_id, person, from_state_id, to_state_id,
  transition_type (正向突破/负向冲击/累积升华/韧性恢复/...),
  trigger_fact_ids (触发转变的多个事实),
  pattern_id (可选，关联到已发现的模式)

emotion_patterns:
  pattern_id, person, description,
  confidence, source_transition_ids,
  created_at, updated_at
```

**情感维度与其他模块的关系：**
```
事件记录池 ← 触发 → 情感状态节点
    ↓                      ↓
时间链 ← 关联 → 情感转变 ← 抽象 → 情感模式
    ↓                      ↓
因果链                   行为预测/情感理解
```

---

### L2：关系层

事实之间、实体之间的关系网络：

| 关系类型 | 说明 | 例子 |
|---------|------|------|
| **时间链** | 时序关系 | "A 发生在 B 之前" |
| **因果链** | 因果关系 | "A 导致了 B" |
| **实体图** | co_occur / 隶属 | "Caroline 和 Melanie 是朋友" |
| **溯源** | 来源追踪 | 这条事实来自 session_3 第5轮 |
| **情感轨迹** | 情感状态的演变路径 | 从"被接纳"到"自豪"到"沮丧"到"满足" |

---

### L3：抽象层（管道）

管道式处理，连接 L1→L2→L4→L5：

| 管道 | 输入 | 输出 | 触发 |
|------|------|------|------|
| **模式发现** | 事件池事实 | 行为模式池 / 情感模式 | 定时 + 情绪转折检测 |
| **知识归纳** | 多个事件事实 | 静态知识池（含 abstracts_from 映射） | 定时 |
| **信念形成** | 模式+知识 | 灵魂层素材 | 定时 |

**抽象管道触发机制：双轨触发**

| 触发方式 | 适用场景 | 时机 | 产出 |
|---------|---------|------|------|
| **情绪转折检测** | 情感层面抽象 | 实时，检测到情绪突变时 | 情感洞察 + 映射 |
| **定时抽象** | 知识层面抽象 | 每 N 条新事实 / 每 M 小时 | 模式归纳 + 映射 |

**情绪转折检测：**
- 检测信号：语气突变、重要事件分享、自我披露、长时间沉默后的情感爆发
- 触发后：回顾近期情节 → LLM 分析情绪模式 → 产出情感抽象事实
- 写入 semantic 池 + 通过 `abstracts_from` 映射回源情节

**定时抽象：**
- 扫描未处理的情节事实
- 按实体/主题聚类 → LLM 分析模式 → 产出知识层面抽象事实
- 写入 semantic/procedural 池 + 映射

---

### L4：叙事层（故事线）

按需动态构建，非静态存储：
- 从 L2 的时间链+因果链+情感轨迹构建故事线
- 不同 query 角度可构建不同叙事
- 有起承转合，有情感弧线

例子：
```
"Caroline 2023年的成长故事：
  第一章：寻找自我（5月）→ 参加支持小组，公开演讲
  第二章：融入社群（6-7月）→ 骄傲游行，归属感上升
  第三章：遭遇挫折（7月）→ 徒步冲突，但韧性恢复
  第四章：新的方向（8-10月）→ 从被接纳到给予支持，领养申请"
```

---

### L5：灵魂层（人物还原）

终极目标——还原出"这个人"：
- 综合的人物理解：核心特质、价值观、人生主线
- 从 L2 关系 + L3 信念 + L4 叙事综合提炼
- 按需动态构建
- 让 Agent 真正"理解"一个人，而不仅仅是检索事实

例子：
```
"Caroline 是一个从自我接纳走向帮助他人的坚韧女性。
  她通过社群参与获得力量，在遭遇挫折后能韧性恢复，
  最终从'寻找接纳'转向'给予支持'——领养孩子是她这一转变的体现。"
```

---

### 遗忘机制（贯穿层）

遗忘不是简单的删除，而是与各层协作的决策：

```
L5 灵魂层 → "这条事实与人物核心特质无关，可降级"
L4 叙事层 → "这条事实已被抽象替代，可归档"
            "这条事实是某个叙事的关键节点，保留"
L3 抽象层 → "这条事实已被归纳成知识，原始细节可降级"
L2 关系层 → "这条事实有很多关系链接，不能随便删"
L1 池     → "低重要性 + 长期未访问 → 标记为可归档"
L0 工作记忆 → "会话结束 → 全部清理或选择性保留"
```

**具体策略：**
- **低重要性归档**：重要性 < 阈值 + 长期未访问 → 移到冷存储
- **相似合并**：多条相似事件 → 合并为一条概括性事实
- **抽象替代**：当 L3 抽象出知识后，原始情节事实可降级（不删，降低权重）

---

## 四、上层能力覆盖

| 上层能力 | 依赖 |
|---------|------|
| ✅ 人物画像 | 静态知识池 + 灵魂层 |
| ✅ 事件回溯 | 事件记录池 + 时间链 |
| ✅ 行为预测 | 行为模式池 |
| ✅ 情感理解 | 情感维度（状态+转变+模式）+ 叙事层 |
| ✅ 关系理解 | 关系层（实体图 + 事实关系） |
| ✅ 知识问答 | 静态知识池 |
| ✅ 建议/推理 | 跨层联合检索 |

---

## 五、讨论记录

| 日期 | 议题 | 决策/内容 |
|------|------|----------|
| 2026-06-12 | Q1 记忆分类 | ✅ 分层（工作/情节/语义/程序记忆） |
| 2026-06-12 | Q2 抽象层次 | ✅ 需要显式抽象管道 |
| 2026-06-12 | Q3 遗忘机制 | ✅ 需要主动遗忘/巩固 |
| 2026-06-12 | Q4 组织方式 | ✅ 时间链 + 因果链 + 实体图 + 溯源 |
| 2026-06-12 | Q5 检索上下文 | ✅ 先 query 驱动，后续扩展 |
| 2026-06-12 | Q6 置信度溯源 | ✅ 需要，部分融入 Q4 |
| 2026-06-12 | Q7 主动学习 | ✅ 加主动反思/整理 |
| 2026-06-12 | 工作记忆-存储内容 | ✅ 原始对话轮次 + 微事实索引（指向原文位置） |
| 2026-06-12 | 工作记忆-检索方式 | ✅ FTS5 关键词匹配（BM25 × 0.7 + 时间衰减 × 0.3） |
| 2026-06-12 | 工作记忆-微事实提取 | ✅ jieba 分词 + 停用词过滤 + 名词性短语规则（纯本地） |
| 2026-06-12 | 工作记忆-检索路由 | ✅ 先工作记忆 → 不足时长期记忆；Agent 可显式指定 |
| 2026-06-12 | 记忆池-划分方式 | ✅ 二维矩阵：时间锚定×具体/模式 → 事件/知识/行为模式三池 |
| 2026-06-12 | 记忆池-存储方式 | ✅ 概念池独立，事实统一存储（type 字段区分） |
| 2026-06-12 | 记忆池-池间关系 | ✅ 多对多映射，通过 fact_relations.abstracts_from 链接 |
| 2026-06-12 | 情感维度 | ✅ 独立维度，非池。存结构化情感轨迹（状态→转变→模式） |
| 2026-06-12 | 抽象管道-触发机制 | ✅ 双轨：情绪转折检测（事件驱动）+ 定时抽象（时间驱动） |
| 2026-06-12 | 层次架构 | ✅ L0 工作记忆 → L1 池 → L2 关系层 → L3 抽象层 → L4 叙事层 → L5 灵魂层 |
| 2026-06-12 | 遗忘机制 | ✅ 贯穿所有层，与各层协作决策 |

---

## 六、外部审阅反馈（2026-06-12）

> 审阅模型：DeepSeek v4 Pro
> 审阅范围：完整架构设计文档

### 6.1 已采纳的改进

| 反馈点 | 决策 |
|--------|------|
| **L4 叙事缓存** | ✅ 采纳。叙事层改为定时构建 + 缓存 + 按需刷新，不每次动态构建 |
| **遗忘 = 精度衰减** | ✅ 采纳。替代阈值删除和冷归档，逐步降低事实的浮点精度（float32→float16→int8→1bit），永不彻底消失 |
| **睡眠周期调度器** | ✅ 采纳。把所有批量操作（巩固、抽象、遗忘、叙事构建）集中到空闲时段，分阶段执行 |
| **灵魂层作为愿景** | ✅ 保留为设计目标（L5），不作为 v2 的交付组件 |
| **情感维度 context_fact_ids 复数** | ✅ 采纳。改为复数，允许多条事实触发一个情感状态 |
| **情感维度初期一表** | ✅ 采纳。初期只用 `emotion_events`，transitions 和 patterns 查询时动态推导，数据量足够后再物化 |

### 6.2 需要进一步讨论的问题

| 反馈点 | 状态 |
|--------|------|
| L0→L1 自动晋升（高频微事实触发 LLM 提取） | ⏳ 实现时讨论 |
| 二维矩阵空洞（时间锚定×行为模式） | ⏳ 待讨论 |
| 遗忘仲裁机制（各层冲突时谁说了算） | ⏳ 待讨论 |
| L3 抽象管道的循环依赖问题 | ⏳ 待讨论 |

### 6.3 已确认的设计方向

| 议题 | 决策 |
|------|------|
| **L2 关系层默认实现** | 时间纠缠（时间邻近度作为隐式关系权重），零成本全覆盖 |
| **时间链** | LLM 提取时标注，相对简单可靠 |
| **因果链** | 需要自我发现机制，不依赖 LLM 提取时标注。时间纠缠提供候选因果对，睡眠周期中验证归纳。**具体实现方案待下次专门讨论** |

### 6.4 值得关注的新想法（未采纳，供参考）

| 想法 | 说明 |
|------|------|
| **递归分形记忆** | 不预设固定层次，让层次从组合关系中涌现 |
| **对抗性记忆巩固** | 巩固器+批评器的 GAN 式抽象质量门控 |
| **反事实记忆** | 存储"差点发生了什么"揭示决策边界 |
| **检索即约束生成** | LLM 先生成"记忆草图"再搜索，迭代优化 |
